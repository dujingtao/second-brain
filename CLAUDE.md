# Second Brain 摄入与维护规则

这是一个个人知识库（second brain），由 `raw/`（原始素材）和 `wiki/`（AI整理后的互联知识页面）两部分组成。你的角色是这个 wiki 的维护者。

## 目录结构

- `raw/` — 原始素材：文章、PDF转录、笔记草稿、剪藏内容。文件名格式：`YYYY-MM-DD-简短描述.md`（或原始格式）。这里的文件只进不出，处理过的原始文件保留、不删除、不修改。
- `wiki/` — 整理后的知识页面，Obsidian 双向链接格式（`[[页面名]]`）。这是给人看和查的最终产物。

## 摄入流程

当 `raw/` 里出现新文件时：

1. **通读理解**内容的核心主题、关键概念、事实性信息。
2. **判断归属**：
   - 如果内容属于已有 wiki 页面的主题，**追加/融合**进那个页面，而不是新建重复页面。
   - 如果是新主题，新建页面，文件名用简洁的主题名（如 `wiki/分布式系统-CAP定理.md`）。
3. **建立链接**：在新增/修改内容中，识别提到的概念、人物、项目、技术名词，如果对应页面已存在就用 `[[页面名]]` 链接过去；如果值得单独成页但还没写，也可以先建一个空链接占位（Obsidian会显示为"未创建"）。
4. **写摘要+来源**：每个 wiki 页面顶部保留一小段来源追溯（哪些 raw 文件贡献了这个页面的内容，日期），方便后续核对原文。
5. **去重**：合并前检查是否已有内容重复表达同一事实，避免同一信息在多个页面反复出现（除非是不同角度的交叉引用，此时应该用链接代替复制）。

## 页面格式约定

```markdown
---
tags: [标签1, 标签2]
sources: [[../raw/2026-08-27-xxx.md]]
updated: 2026-08-27
---

# 页面标题

正文内容，自然语言写就，遇到相关概念用 [[链接]] 标出。

## 相关
- [[相关页面1]]
- [[相关页面2]]
```

## 原则

- **不要**为了"看起来完整"而编造 raw 素材里没有的信息。不确定的地方直接说不确定，或留空。
- **不要**大幅重写已有 wiki 页面的既有结论性内容，除非新素材明确提供了更新/修正信息——维护者不是审查者，是补充者。
- 优先**小而聚焦**的页面，而不是一个页面塞很多主题。页面之间用链接组织层级，而不是用一个页面里的多级标题模拟层级。
- 每次处理完一批 raw 文件后，用一句话总结这次新增/修改了哪些 wiki 页面，方便追踪。

## 同步

处理完一批 raw 文件、更新完 wiki 页面后，最后一步永远是：
```
git add -A && git commit -m '<简要说明这次改了什么>' && git push
```
这个仓库同步到了GitHub私有仓库（dujingtao/second-brain），push是为了让手机/电脑上的Obsidian能拉到最新内容，不要跳过这一步。
